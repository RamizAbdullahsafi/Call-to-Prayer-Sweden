# Copy to ".env" or ".env.local" to override. The repo ships ".env.production"
# with the public Netlify URL so Android/Capacitor hits a real `/api/bonetider`.
# For a fork, change the URL to your deployed site (no trailing slash).
# VITE_API_ORIGIN=https://call-to-prayer-sweden.netlify.app

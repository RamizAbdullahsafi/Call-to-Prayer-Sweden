# Copy to ".env" or ".env.production" for release builds.
# Android needs a real HTTPS origin so bönetider requests work when the app is closed
# (background refresh + scheduling). Replace with your deployed site.
# VITE_API_ORIGIN=https://your-app.netlify.app
